import React from 'react';
import { NavigationContainer } from '@react-navigation/native';

import StackManegment from './companent/StackManegment';


const Router = () => {
  

  return (
    <NavigationContainer>
      <StackManegment />
    </NavigationContainer>
  );
};

export default Router;
